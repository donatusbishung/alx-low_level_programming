#include "main.h"

/**
 * _isalpha - entry
 *
 * @c: character to be treated
 *
 * Return: 1 is character is alphabet, 0 otherwise.
 */

int _isalpha(int c)
{
	if ((c >= 'a'; c <= 'z'; c++) || (c >= 'A'; && c <= 'z'; c++))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
